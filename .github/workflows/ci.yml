name: CI Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

env:
  PYTHON_VERSION: "3.12"

jobs:
  code-quality:
    name: Code Quality (Ruff & Black)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          
      - name: Install Linters
        run: pip install ruff black

      - name: Run Ruff
        run: ruff check .

      - name: Run Black
        run: black --check .

  backend-tests:
    name: Backend Tests - ${{ matrix.service }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false # Continues testing all services even if one fails
      matrix:
        service:
          - auth_service
          - profile_service
          - listings_service
          - building_service
          - application_service
          - reviews_service
          - chat_service
          - notification_service
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install Service Dependencies
        working-directory: ./${{ matrix.service }}
        run: |
          # Install production requirements
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Install testing libraries
          pip install pytest pytest-django pytest-cov

      - name: Run Pytest and Generate Coverage
        id: run_tests
        working-directory: ./${{ matrix.service }}
        env:
          DJANGO_SETTINGS_MODULE: config.settings
          SECRET_KEY: test-secret-key
        run: |
          pytest --cov=. --cov-report=xml --junitxml=pytest-report.xml -v

      - name: Handle Test Failure Output
        if: failure() && steps.run_tests.outcome == 'failure'
        run: |
          echo "::error::Service ${{ matrix.service }} failed tests."
          echo "Pytest output should be reviewed in the logs above."
          cat ./${{ matrix.service }}/pytest-report.xml || true

      - name: Upload Coverage Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-${{ matrix.service }}
          path: ./${{ matrix.service }}/coverage.xml
          retention-days: 14

      - name: Upload Pytest Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pytest-report-${{ matrix.service }}
          path: ./${{ matrix.service }}/pytest-report.xml
          retention-days: 14

  coverage:
    name: Global Coverage
    needs: [backend-tests]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Download all coverage artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-*
          merge-multiple: true

      - name: Install Coverage
        run: pip install coverage

      - name: Generate Global Coverage XML
        run: |
          coverage combine || echo "No coverage data to combine"
          coverage xml -o coverage.xml || echo "Failed to generate XML"

      - name: Upload Global Coverage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: global-coverage
          path: coverage.xml
          retention-days: 14

  docker-build:
    name: Docker Build Validation - ${{ matrix.service }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - auth_service
          - profile_service
          - listings_service
          - building_service
          - application_service
          - reviews_service
          - chat_service
          - notification_service
          - gateway
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.service }}
          push: false
          tags: ${{ matrix.service }}:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
